/**
 * This function returns a phrase based on the value of x^2
 * @param {integer} x 
 * @returns a string
 */
function apple(x){
    return x**2 > 1000 ? "It's hotter than the sun!!" : "Help yourself to a honeycomb Yorkie for the glovebox."
  }